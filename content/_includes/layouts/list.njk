---
title: vinckr lists
year: 2024
author: vinckr
---

{% include "./header.njk" %}
{% block content %}
  <body class="dark:bg-[#111010]">
    <div class="max-w-4xl mb-40 flex flex-col md:flex-row mx-4 mt-8 md:mt-20 lg:mt-32 lg:mx-auto">
      {% include "./sidebar.njk" %}
      <main class="flex-auto min-w-0 mt-6 md:mt-0 flex flex-col md:px-0">
        <h1 class="prose text-4xl dark:prose-invert font-extrabold">{{ title }}</h1>
        <div class="prose prose-zinc md:prose-base lg:prose-lg dark:prose-invert">
          <div>
            {{ content | safe }}
          </div>
          <div>
            <ul>
              {% for item in wikipedia %}
                <li>
                  <a href="{{ item.url }}">{{ item.title }}</a>
                </li>
              {% endfor %}
            </div>
          </div>
        </main>
      </div>
      <footer class="flex justify-end items-center pr-32">
        <span class="mr-4 prose dark:prose-invert">All Rites Reversed Ⓚ {{ year }} by {{ author }}</span>
        <img src="/img/nekojiru.gif" alt="Nekojiru"></footer>
      </body>
    </html>
  {% endblock %}
</body>